{
    "message_rules": [
        "// Each entry is one line. Blank, # and // lines are skipped.",
        "// Use `let <name> = <expr>` to declare reusable subtrees, then",
        "// reference them with ${name}. Exactly one rule line is required",
        "// and it must be last.",
        "true"
    ],
    "endorsements": [
    ],
    "rm_servers": [
        "127.0.0.1:3939"
    ],
    "key_file": "keys/es.pem"
}
